# Dev-only Forge manifest for the Quick Skin E2E harness. Never shipped in the release jar.
modLoader = "javafml"
loaderVersion = "[47,)"
license = "All Rights Reserved"

[[mods]]
modId = "quick_skin_e2e"
version = "0.0.0"
displayName = "Quick Skin E2E (dev-only)"
description = "Dev-only end-to-end test harness for Quick Skin."
# Client-only automation mod connecting to a server that intentionally lacks it.
displayTest = "IGNORE_ALL_VERSION"

[[dependencies.quick_skin_e2e]]
modId = "quickskin"
mandatory = true
versionRange = "[0,)"
ordering = "AFTER"
side = "BOTH"

[[dependencies.quick_skin_e2e]]
modId = "forge"
mandatory = true
versionRange = "[47,)"
ordering = "NONE"
side = "BOTH"
